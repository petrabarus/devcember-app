version: 0.1

phases:
    install:
        commands:
            - apt-get update -y
            - apt-get install -y npm
            - npm install
    build:
        commands:
            - npm run build

    post_build:
        commands:
            - echo "Upload to Lambda"
